# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Harness local (Claude Code) — específico del entorno
.claude/settings.local.json

# SO
.DS_Store
Thumbs.db

# Payloads de Fase 2: regenerables con `extract.py --prepare` (evitar bloat)
data/fetched/*/_payload_*.txt

# Insumo externo del canal A.15 (archivo grande, no versionar)
data/external/*.xlsx
