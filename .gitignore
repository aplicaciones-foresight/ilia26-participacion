# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Harness local (Claude Code) — específico del entorno
.claude/settings.local.json

# SO
.DS_Store
Thumbs.db

# Payloads de Fase 2: regenerables con `extract.py --prepare` (evitar bloat)
data/fetched/*/_payload_*.txt

# Scratch de descubrimiento amplio (JSON crudos por cluster); se sintetizan en
# fichas + reporte committeados. No versionar los intermedios.
data/_discovery/
